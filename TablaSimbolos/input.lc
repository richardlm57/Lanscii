{ %foo bar !h @hola chao e|
    bar = 3;
    write hola;
    h = false;
    {- asignar a foo el doble del valor de bar, más treinta y cinco;
        si bar es mayor a dos -}
    ( bar > 2 ?
        foo = 35 + bar * 2
    );
    e=$hola~chao;
    [foo > 2 | foo = 33]
}
